---

---

<section id="entry" class="recruit-cta u-bg-primary-light">
  <div class="recruit-cta__inner">
    <h2 class="sr-only">エントリー</h2>
    <a class="recruit-cta__button" href="/contact">エントリーはこちら</a>
    <p class="recruit-cta__sub">お待ちしております。</p>
  </div>
</section>

<style>
  /* ===== 7. エントリーCTA（recruit-cta） ===== */
  /* Figma: 500×100 ボタン, bg #448AAC, border 1px #5C5C5C, rounded 15px */

  .recruit-cta__inner {
    padding-block: var(--space-8);
    display: grid;
    justify-items: center;
    gap: var(--space-4);
  }

  .recruit-cta__button {
    --recruit-cta-button-font-size: clamp(
      1.25rem,
      calc(2.380952381vw + 0.1071428571rem),
      2.25rem
    );

    display: grid;
    place-items: center;
    width: min(100%, 13.8888888889em);
    padding: 0.6666666667em 0.8888888889em;
    background-color: var(--color-primary);
    border: 1px solid var(--color-gray-500);
    border-radius: 0.4166666667em;
    font-family: var(--font-serif);
    font-size: var(--recruit-cta-button-font-size);
    font-weight: var(--weight-semibold);
    color: var(--color-primary-light);
    text-decoration: none;
    text-align: center;
    text-shadow: none;
  }

  .recruit-cta__sub {
    font-family: var(--font-serif);
    font-size: var(--fs-300);
    font-weight: var(--weight-semibold);
    color: var(--color-black);
    margin: 0;
  }
</style>
