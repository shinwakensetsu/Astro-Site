---
import Layout from "../layouts/Layout.astro";
import ShinwaLogo from "../components/ShinwaLogo.astro";
import IconArrow from "../components/IconArrow.astro";
import WorksCarousel from "../components/WorksCarousel.astro";
import { getTopPage, getNews, type News } from "../lib/microcms";
import { optimizeImageUrl, imagePresets } from "../utils/imageOptimizer";

const [topPage, newsResponse] = await Promise.all([
  getTopPage(),
  getNews({ orders: "-publishedDate", limit: 5 }).catch(() => ({
    contents: [] as News[],
  })),
]);
const newsList = newsResponse.contents;
---

<Layout>
  <main>
    <!-- ===================== 1. Hero ===================== -->
    <section class="hero" id="hero" aria-label="トップビジュアル">
      {
        topPage?.heroImage && (
          <img
            class="hero__img"
            src={optimizeImageUrl(topPage.heroImage.url, imagePresets.hero)}
            alt=""
            width={topPage.heroImage.width}
            height={topPage.heroImage.height}
            loading="eager"
            fetchpriority="high"
          />
        )
      }

      <div class="hero__inner">
        <!-- ロゴ: 上中央 -->
        <div class="hero__logo-wrap">
          <ShinwaLogo />
        </div>

        <!-- コピー: 左上 -->
        <div class="hero__copy">
          <p class="hero__copy-main" data-sr-join>
            社寺建設の継承と更なる<span class="hero__copy-break"
              >発展をめざして</span
            >
          </p>
          <p class="hero__copy-sub">
            受け継がれた高度な大工技術や文化的価値の高い施工力を。
          </p>
        </div>
      </div>
    </section>

    <!-- ===================== 2. お知らせ ===================== -->
    <section class="news" id="news" aria-labelledby="news-heading">
      <div class="news__header">
        <div class="l-container news__header-inner">
          <h2 class="news__heading" id="news-heading">お知らせ</h2>
        </div>
      </div>
      <div class="news__body">
        <div class="l-container news__body-inner">
          {
            newsList.length > 0 ? (
              <ul class="news__list">
                {newsList.map((item) => (
                  <li class="news__card">
                    <a class="news__card-link" href={`/news/${item.id}`}>
                      <div class="news__card-thumb">
                        {item.thumbnail ? (
                          <img
                            class="news__card-img"
                            src={optimizeImageUrl(item.thumbnail.url, {
                              width: 180,
                              height: 180,
                              fit: "crop",
                              format: "auto",
                              quality: 80,
                            })}
                            alt=""
                            width="180"
                            height="180"
                            loading="lazy"
                          />
                        ) : (
                          <div
                            class="news__card-thumb news__card-thumb--placeholder"
                            aria-hidden="true"
                          />
                        )}
                      </div>
                      <div class="news__card-body">
                        <p class="news__card-title">{item.title}</p>
                        {item.excerpt && (
                          <p class="news__card-excerpt">{item.excerpt}</p>
                        )}
                      </div>
                    </a>
                  </li>
                ))}
              </ul>
            ) : (
              <p class="news__empty">現在お知らせはありません。</p>
            )
          }
          <a
            class="arrow-link arrow-link--on-dark news__more"
            href="/news"
            data-sr-join
          >
            <span class="arrow-link__text">
              <span>お知らせは</span>
              <span>こちら</span>
            </span>
            <IconArrow width="72" height="72" />
          </a>
        </div>
      </div>
    </section>

    <!-- ===================== 3. 継承と進化 ===================== -->
    <section class="heritage" id="heritage" aria-labelledby="heritage-heading">
      <!-- 上部: 五重塔エリア -->
      <div class="heritage__tower-area">
        <h2 class="heritage__heading" id="heritage-heading">
          <span class="heritage__heading-line heritage__heading-line--black"
            >継承と</span
          >
          <span
            class="heritage__heading-line heritage__heading-line--vertical heritage__heading-line--white-shadow"
            >進化</span
          >
        </h2>

        <div class="heritage__skew-container">
          <div class="heritage__skew-inner">
            <div
              class="heritage__tower-wrap"
              style="--tower-bg: url('/images/index/tower.jpg');"
            >
              <div class="heritage__tower-frame">
                <img
                  class="heritage__tower-img"
                  src="/images/index/tower.jpg"
                  alt="五重塔"
                  width="2088"
                  height="2690"
                  loading="lazy"
                />
                <div class="heritage__tower-overlay">
                  <div class="heritage__tower-copy">
                    <span class="heritage__vertical-title" aria-hidden="true"
                      >私たちについて</span
                    >

                    <div class="heritage__tower-text-group">
                      <div class="heritage__overlay-text">
                        <p>
                          現在の社寺建築の様式および技術は、日本在来の神社建築と仏教伝来による寺院建築様式が融合し、国内でのたゆみない進化と各時代における大陸からの技術、特に鎌倉時代の禅宗様大仏様という新様式ながら、江戸時代にほぼ確立しました。明治時代以降、堂宮大工はその技術・優秀な道具類により過去最高の技能を発揮してきました。
                        </p>
                      </div>

                      <div class="heritage__tower-caption">
                        <p>
                          伸和建設は、現在まで、<strong>150年</strong
                          >にわたり有数の社寺建築業者として、<strong
                            >社寺建築史上に残る</strong
                          >多数の作品を造り上げてきました。
                        </p>
                        <p>
                          古来から現在まで大工、棟梁によって永々と引継がれ伝統的木造工法を強みに社寺建築を残す
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- 下部: 画像2枚 + テキスト -->
            <div class="heritage__bottom">
              <div class="heritage__bottom-images">
                <img
                  class="heritage__bottom-img"
                  src="/images/index/pillar.jpg"
                  alt="社寺建築の木組み構造"
                  width="1000"
                  height="1206"
                  loading="lazy"
                />
                <img
                  class="heritage__bottom-img"
                  src="/images/index/wood.jpg"
                  alt="伝統的な継手・仕口"
                  width="1032"
                  height="708"
                  loading="lazy"
                />
              </div>
              <div class="heritage__bottom-text">
                <p>
                  当社の名の由来は、昭和23年の会社設立に当たり、伝統和風建築（和）の継承と言う私たちの日頃の立場を守り伸ばして発展（伸）させることを念頭において伸和建設株式会社と命名しました。
                </p>
                <p>
                  今後とも、多くのお施主様をはじめ関係者の皆様のご支援により当社が成り立ってまいりましたことを思い、社寺建築の継承とさらなる発展を目指していきます。
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="heritage__content">
        <div class="l-container heritage__content-inner">
          <a
            class="arrow-link arrow-link--on-dark heritage__more"
            href="/company"
            data-sr-join
          >
            <span class="arrow-link__text">
              <span>企業情報は</span>
              <span>こちら</span>
            </span>
            <IconArrow width="72" height="72" />
          </a>
        </div>
      </div>
    </section>

    <!-- ===================== 3.5 仕事内容 ===================== -->
    <section class="services" id="services" aria-labelledby="services-heading">
      <div class="l-container services__inner">
        <h2 class="services__heading" id="services-heading">仕事内容</h2>

        <div class="services__content">
          <div class="services__banners">
            <div
              class="services__banner"
              style="--banner-bg: url('/images/index/chiseling.jpg');"
            >
              <div class="services__banner-inner">
                <span class="services__banner-label">掘る</span>
                <span class="services__banner-dash" aria-hidden="true"></span>
              </div>
            </div>
            <div
              class="services__banner"
              style="--banner-bg: url('/images/index/hammering.jpg');"
            >
              <div class="services__banner-inner">
                <span class="services__banner-label">打ち</span>
                <span class="services__banner-dash" aria-hidden="true"></span>
              </div>
            </div>
            <div
              class="services__banner"
              style="--banner-bg: url('/images/index/planing.jpg');"
            >
              <div class="services__banner-inner">
                <span class="services__banner-label">削り</span>
                <span class="services__banner-dash" aria-hidden="true"></span>
              </div>
            </div>
            <div
              class="services__banner"
              style="--banner-bg: url('/images/index/finishing.jpg');"
            >
              <div class="services__banner-inner">
                <span
                  class="services__banner-label"
                  style="color: var(--color-black);">整え</span
                >
                <span
                  class="services__banner-dash"
                  style="background-color: var(--color-black);"
                  aria-hidden="true"></span>
              </div>
            </div>
          </div>

          <div class="services__lead">
            <h3 class="services__lead-title">伸和建設の想い</h3>
            <p class="services__lead-text">
              時代に迎合するだけでなく残すべき技術や生産システムを守り正常な社寺建築の進化を図り後世にそれを残していくことが責務だと考えています。
            </p>
          </div>
        </div>

        <a
          class="arrow-link arrow-link--on-light services__more"
          href="/services"
          data-sr-join
        >
          <span class="arrow-link__text">
            <span>仕事内容詳細は</span>
            <span>こちら</span>
          </span>
          <IconArrow width="72" height="72" />
        </a>
      </div>
    </section>

    <!-- ===================== 4. 施工事例 ===================== -->
    <section class="works" id="works" aria-labelledby="works-heading">
      <div class="works__header">
        <h2 class="works__heading" id="works-heading">施工事例</h2>
      </div>
      <WorksCarousel />
    </section>

    <!-- ===================== 5. Recruit ===================== -->
    <section class="recruit" id="recruit" aria-labelledby="recruit-heading">
      <div class="l-container recruit__inner">
        <div
          class="recruit__card"
          style="--recruit-bg: url('/images/index/recruit-bg.jpg');"
        >
          <div class="recruit__intro">
            <h2 class="recruit__heading" id="recruit-heading">RECRUIT</h2>
            <p class="recruit__desc">
              <span class="recruit__desc-line"
                >ただのものづくりではなく、歴史と文化を守る仕事があります。</span
              >
              <span class="recruit__desc-line"
                >親しみやすい社風の中で、伝統技術を磨き、次世代へ伝えていく。</span
              >
              <span class="recruit__desc-line"
                >まずはお気軽に話を聞きに来てください。</span
              >
            </p>
          </div>

          <p class="recruit__catchphrase">
            伝統と革新が出会う<span class="recruit__catchphrase-break"
              >職人の舞台</span
            >
          </p>

          <a class="recruit__cta" href="#">
            <span class="recruit__cta-text">Join Our Team?</span>
            <IconArrow width="72" height="72" />
            <span class="recruit__cta-label">RECRUIT<br />SITE</span>
          </a>
        </div>

        <p class="recruit__note">
          <span class="recruit__note-line"
            >伸和建設株式会社に興味をお持ちいただきありがとうございます。</span
          >
          <span class="recruit__note-break"
            >当社求人に応募の方は、募集要項および採用条件を確認の上必要事項をフォームに入力いただき送信ボタンを押してください。</span
          ><span class="recruit__note-break"
            >なお、収集した個人情報は、本求人の目的以外にはご使用いたしません。</span
          >
        </p>
      </div>
    </section>

    <!-- ===================== 6. Contact Us ===================== -->
    <aside class="contact" id="contact" aria-labelledby="contact-heading">
      <a class="l-container contact__inner" href="/contact">
        <div class="contact__card">
          <div class="contact__title-group">
            <h2 class="contact__heading" id="contact-heading">Contact Us</h2>
            <span class="contact__heading-sub">お問い合わせ</span>
          </div>

          <p class="contact__desc">
            弊社サービスにご興味をお持ちの方はどのようなご質問・ご相談でも構いません。
            <span class="contact__desc-break"
              >まずはお気軽にご相談ください。</span
            >
          </p>

          <div class="contact__more">
            <IconArrow width="72" height="72" />
          </div>
        </div>
      </a>
    </aside>

    <!-- ===================== 7. [退避] microCMS データ ===================== -->
    <!--
  利用予定:
    topPage.heroTitle       → 未使用（コピーは直書き）
    topPage.heroDescription → 未使用（コピーは直書き）
  -->
  </main>
</Layout>

<style>
  :global(html),
  :global(body) {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  :global(html::-webkit-scrollbar),
  :global(body::-webkit-scrollbar) {
    width: 0;
  }

  /* ---------- 共通: arrow-link ---------- */
  .arrow-link {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    font-size: clamp(var(--text-sm), 2.5vw, var(--text-xl));
    text-decoration: none;
    max-width: 100%;
    --icon-arrow-circle-fill: var(--color-primary-light);
    --icon-arrow-path-stroke: var(--color-gray-500);
    --icon-arrow-border: rgba(0, 0, 0, 0.5);
    --icon-arrow-circle-fill-hover: var(--color-gray-500);
    --icon-arrow-path-stroke-hover: var(--color-primary-light);
    --icon-arrow-border-hover: rgba(255, 255, 255, 0.5);
  }

  .arrow-link:hover {
    text-decoration: none;
  }

  .arrow-link__text {
    display: flex;
    flex-direction: column;
    line-height: var(--leading-tight);
    text-align: center;
    min-width: 0;
  }

  .arrow-link__text span {
    overflow-wrap: break-word;
  }

  .arrow-link :global(svg) {
    width: clamp(32px, 4.6875vw, 75px);
    height: auto;
    min-width: 32px;
    min-height: 32px;
    flex-shrink: 1;
  }

  /* 黒背景用 */
  .arrow-link--on-dark {
    color: var(--color-white);
  }

  /* 白背景用 */
  .arrow-link--on-light {
    color: var(--color-dark);
    --icon-arrow-circle-fill: var(--color-gray-500);
    --icon-arrow-path-stroke: var(--color-primary-light);
    --icon-arrow-border: rgba(255, 255, 255, 0.5);
    --icon-arrow-circle-fill-hover: var(--color-primary-light);
    --icon-arrow-path-stroke-hover: var(--color-gray-500);
    --icon-arrow-border-hover: rgba(0, 0, 0, 0.5);
  }
  /* ---------- 1. Hero ---------- */
  .hero {
    position: relative;
    width: 100%;
    max-width: 100%;
    display: grid;
    background-color: var(--color-dark);
    line-height: 0;
  }

  .hero__inner {
    grid-area: 1 / 1;
    position: relative;
    display: flex;
    flex-direction: column;
    padding-bottom: var(--space-4);
  }

  .hero__img {
    grid-area: 1 / 1;
    display: block;
    width: 100%;
    height: auto;
    max-height: 90svh;
    object-fit: cover;
    object-position: center;
  }

  /* ロゴ: 上中央 — 全幅で不変 */
  .hero__logo-wrap {
    position: absolute;
    top: 5%;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
    color: rgba(255, 255, 255, 0.6);
  }

  .hero__logo-wrap :global(svg) {
    width: clamp(110px, 15.5vw, 223px);
    height: auto;
  }

  /* コピー: 1440px超は左上 / 1440px以下は左下 / 768px以下はロゴ下に縦積み */
  .hero__copy {
    margin-top: clamp(20px, 5vw, 60px);
    margin-left: clamp(16px, 5vw, 60px);
    line-height: var(--leading-tight);
    z-index: 1;
    max-width: 80vw;
    word-break: auto-phrase;
  }

  @media (max-width: 1440px) {
    .hero__copy {
      margin-top: auto;
      margin-bottom: clamp(20px, 5vw, 60px);
      background: linear-gradient(0deg, #00000080, transparent);
    }
  }

  @media (max-width: 768px) {
    .hero__img {
      height: 300px;
    }

    .hero__logo-wrap {
      top: 10px;
    }

    .hero__copy {
      margin-top: calc(70px + clamp(50px, 7vw, 100px) + var(--space-4));
      margin-bottom: 0;
    }
  }

  .hero__copy-main {
    font-size: clamp(1.5rem, 5vw, 3.75rem);
    line-height: var(--leading-tight);
    color: var(--color-white);
  }

  .hero__copy-break {
    display: block;
  }

  .hero__copy-sub {
    font-size: clamp(0.75rem, 1.5vw, 1.125rem);
    line-height: var(--leading-relaxed);
    margin: 0;
    color: var(--color-white);
  }

  /* ---------- 2. News ---------- */
  .news {
    width: 100%;
    background-color: var(--color-dark);
    display: flex;
    flex-direction: column;
    container-type: inline-size;
  }

  .news__header {
    flex-shrink: 0;
    min-height: 3rem;
    background-color: var(--color-primary-light);
    display: flex;
    align-items: center;
    padding-block: var(--space-1);
  }

  .news__header-inner {
    padding-inline: var(--space-6);
  }

  .news__heading {
    font-size: var(--text-2xl);
    line-height: 1;
    margin: 0;
    color: var(--color-dark);
    text-align: end;
  }

  /* デフォルト: 縦並びレイアウト */
  .news__body {
    flex: 1;
    min-height: clamp(150px, 20vw, 300px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding-inline: var(--space-6);
    padding-block: var(--space-4);
  }

  .news__body-inner {
    box-sizing: border-box;
    --news-thumb-size: clamp(120px, 24vw, 180px);
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: auto auto;
    grid-template-areas:
      "list"
      "more";
    row-gap: var(--space-3);
    justify-items: stretch;
    align-content: center;
  }

  .news__list {
    list-style: none;
    margin: 0;
    padding: 0;
    width: 100%;
    min-width: 0;
    display: flex;
    flex-direction: row;
    gap: var(--space-4);
    align-items: center;
    justify-content: center;
    align-self: start;
    grid-area: list;
  }

  .news__card {
    flex: 0 0 auto;
    width: min(360px, calc((100% - var(--space-4)) / 2));
    min-width: 0;
    max-width: 100%;
    margin-inline: 0;
  }

  .news__card-link {
    display: flex;
    flex-wrap: wrap;
    min-width: 0;
    flex-direction: row;
    gap: var(--space-2);
    text-decoration: none;
    color: var(--color-white);
    border-radius: var(--radius-md);
    transition: background-color var(--duration-fast) var(--easing-default);
  }

  .news__card-link:hover {
    background-color: rgba(255, 255, 255, 0.12);
  }

  .news__card-thumb {
    flex-shrink: 0;
    width: var(--news-thumb-size);
    max-width: 100%;
    height: var(--news-thumb-size);
    align-self: center;
    border-radius: 20px 0 0 0;
    overflow: hidden;
    filter: drop-shadow(8px 8px 0 rgba(255, 255, 255, 1));
  }

  .news__card-img {
    display: block;
    width: var(--news-thumb-size);
    height: var(--news-thumb-size);
    object-fit: cover;
  }

  .news__card-thumb--placeholder {
    width: var(--news-thumb-size);
    max-width: 100%;
    height: var(--news-thumb-size);
    background-color: var(--color-gray-700);
  }

  .news__empty {
    color: var(--color-gray-400);
  }

  .news__more {
    align-self: start;
    grid-area: more;
    justify-self: end;
    width: fit-content;
    max-width: min(220px, 100%);
  }

  .news__card-body {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: var(--space-2);
    padding: var(--space-2);
  }

  .news__card-title {
    font-size: var(--text-base);
    line-height: var(--leading-tight);
    margin: 0;
    display: block;
    overflow-wrap: break-word;
  }

  .news__card-excerpt {
    font-size: var(--text-sm);
    line-height: var(--leading-normal);
    margin: 0;
    color: var(--color-gray-400);
    overflow-wrap: break-word;
  }

  @container (max-width: 768px) {
    .news__body-inner {
      --news-thumb-size: 3.75rem;
    }

    .news__list {
      flex-direction: column;
      align-items: flex-start;
      justify-content: flex-start;
      gap: var(--space-3);
    }

    .news__card {
      width: min(320px, 100%);
    }
  }

  /* ---------- 3. Heritage ---------- */
  .heritage {
    --heritage-heading-offset-x: clamp(1rem, 2.8vw, 2.75rem);
    --heritage-heading-offset-y: clamp(2rem, 6vw, 5rem);
    --heritage-heading-reserve: clamp(5.5rem, 12vw, 9rem);
    width: 100%;
    background-image: url("/images/util/noise-tile-300.svg");
    background-repeat: repeat;
    background-size: 300px 300px;
    position: relative;
    z-index: 1;
  }

  .heritage__tower-area {
    position: relative;
    padding-top: var(--heritage-heading-reserve);
  }

  /* 画像に重ねて表示する見出しのため absolute */
  .heritage__heading {
    font-size: clamp(3.5rem, 8.5vw, 10rem);
    line-height: 1.15;
    color: var(--color-white);
    margin: 0;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 2;
    width: fit-content;
    max-width: calc(100% - var(--heritage-heading-offset-x));
    overflow-wrap: break-word;
    transform: translate(
      var(--heritage-heading-offset-x),
      var(--heritage-heading-offset-y)
    );
    pointer-events: none;
  }

  .heritage__heading-line {
    display: block;
  }

  .heritage__heading-line--black {
    color: var(--color-black);
  }

  .heritage__heading-line--vertical {
    display: inline-block;
    writing-mode: vertical-rl;
    text-orientation: upright;
    line-height: 1;
  }

  .heritage__heading-line--white-shadow {
    text-shadow: 0.25em 0.3125em 0.04375em rgba(255, 255, 255, 0.8);
  }

  .heritage__skew-container {
    background-color: var(--color-dark);
    position: relative;
    clip-path: polygon(0 clamp(50px, 11vw, 170px), 100% 0, 100% 100%, 0 100%);
  }

  .heritage__vertical-title {
    writing-mode: vertical-rl;
    font-size: var(--text-heritage-title);
    line-height: 1.2;
    letter-spacing: 0.12em;
    color: var(--color-white);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.22em;
    margin: auto;
    max-width: 100%;
    overflow-wrap: break-word;
    position: relative;
    top: 2rem;
  }

  .heritage__overlay-text {
    color: rgba(255, 255, 255, 0.85);
    font-size: clamp(var(--text-base), 1.4vw, var(--text-heritage-body));
    line-height: var(--leading-relaxed);
    line-break: strict;
    text-align: start;
    overflow-wrap: break-word;
    min-height: 15.3125rem;
    padding-top: var(--space-4);
    padding-inline: 10%;
    margin: auto;
  }

  @media (min-width: 769px) {
    .heritage__overlay-text {
      writing-mode: vertical-rl;
      direction: ltr;
      text-orientation: mixed;
      height: 15.3125em;
      min-height: auto;
      max-width: 100%;
      overflow-x: auto;
      padding-top: 0;
      padding-inline: 0;
    }
  }

  .heritage__tower-wrap {
    position: relative;
    width: min(1024px, 100%);
    max-width: 100%;
    margin-inline: auto;
    isolation: isolate;
  }

  /* 背景ぼかし影: タワー画像の外周にブラー効果を出すため absolute */
  .heritage__tower-wrap::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: var(--tower-bg);
    background-size: cover;
    background-position: center;
    border-radius: 0 0 0 20px;
    filter: blur(8px);
    opacity: 0.4;
    z-index: -1;
    transform: scale(1.05);
  }

  .heritage__tower-frame {
    display: grid;
    border-radius: 0 0 0 20px;
  }

  .heritage__tower-img,
  .heritage__tower-overlay {
    grid-area: 1 / 1;
  }

  .heritage__tower-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.8);
    z-index: 0;
    border-radius: 0 0 0 20px;
  }

  .heritage__tower-overlay {
    z-index: 1;
    min-width: 0;
  }

  .heritage__tower-copy {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    margin-top: 2.5rem;
    top: -2.5rem;
    position: relative;
  }

  .heritage__tower-copy > * {
    min-width: 0;
  }

  .heritage__tower-text-group {
    margin-top: auto;
    min-width: 0;
  }

  .heritage__tower-caption {
    width: 100%;
    background-color: rgba(0, 0, 0, 0.6);
    border-radius: 0 0 0 20px;
    color: var(--color-white);
    font-size: var(--text-heritage-caption);
    line-height: var(--leading-normal);
    margin-top: var(--space-4);
    padding-block: 2rem;
  }

  .heritage__tower-caption p {
    margin: 0;
    padding-inline: 6%;
    overflow-wrap: break-word;
  }

  .heritage__tower-caption p + p {
    margin-top: var(--space-8);
  }

  .heritage__tower-caption strong {
    color: var(--color-gold);
    font-weight: var(--weight-bold);
  }

  @media (max-width: 1024px) {
    .heritage__tower-caption {
      font-size: var(--text-xl);
    }
    .heritage__tower-wrap::after {
      transform: scale(1);
    }

    .heritage__tower-caption p + p {
      margin-top: var(--space-4);
    }
  }

  @media (max-width: 768px) {
    .heritage {
      --heritage-heading-offset-x: var(--space-4);
      --heritage-heading-offset-y: clamp(1.75rem, 7vw, 3.5rem);
      --heritage-heading-reserve: clamp(4.75rem, 14vw, 7rem);
    }

    .heritage__tower-caption {
      font-size: var(--text-lg);
    }
  }

  @media (max-width: 425px) {
    .heritage {
      --heritage-heading-offset-x: var(--space-3);
      --heritage-heading-offset-y: clamp(1.25rem, 6vw, 2.25rem);
      --heritage-heading-reserve: clamp(4rem, 16vw, 5.5rem);
    }

    .heritage__tower-caption {
      font-size: var(--text-base);
      line-height: var(--leading-relaxed);
    }

    .heritage__tower-caption p + p {
      margin-top: var(--space-2);
    }
  }

  .heritage__content {
    background-color: var(--color-dark);
  }

  .heritage__content-inner {
    padding-inline: var(--space-6);
    padding-block: var(--space-5);
    margin-block: calc(var(--space-2) * -1);
    bottom: 1px;
    position: relative;
  }

  .heritage__more {
    width: fit-content;
    margin-left: auto;
  }

  /* --- 下部: 画像2枚 + テキスト --- */
  .heritage__bottom {
    --heritage-bottom-inline-space: clamp(1rem, 4vw, 2rem);
    --heritage-bottom-column-gap: clamp(1.5rem, 5vw, 50px);
    --heritage-bottom-row-gap: clamp(0.75rem, 2vw, 20px);
    display: grid;
    width: min(1024px, calc(100% - (var(--heritage-bottom-inline-space) * 2)));
    height: clamp(320px, 31.25vw, 600px);
    margin-inline: auto;
    margin-top: var(--space-6);
    padding-bottom: var(--space-8);
    grid-template-columns: repeat(2, minmax(0, 1fr));
    grid-template-rows: repeat(2, minmax(0, 1fr));
    column-gap: var(--heritage-bottom-column-gap);
    row-gap: var(--heritage-bottom-row-gap);
    box-sizing: border-box;
    z-index: 1;
    position: relative;
  }

  .heritage__bottom-images {
    display: contents;
  }

  .heritage__bottom-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .heritage__bottom-img:first-child {
    grid-column: 1 / 2;
    grid-row: 1 / 3;
    border-radius: 0 50px 0 0;
  }

  .heritage__bottom-img:last-child {
    grid-column: 2 / 3;
    grid-row: 1 / 2;
    border-radius: 0 0 0 50px;
  }

  .heritage__bottom-text {
    grid-column: 2 / 3;
    grid-row: 2 / 3;
    min-width: 0;
    color: rgba(255, 255, 255, 0.85);
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
  }

  .heritage__bottom-text p {
    margin: 0;
  }

  .heritage__bottom-text p + p {
    margin-top: var(--space-1);
  }

  /* heritage__bottom レスポンシブ: 768px以下 → 1カラム縦積み */
  @media (max-width: 768px) {
    .heritage__bottom {
      --heritage-bottom-mobile-image-height: clamp(220px, 48vw, 360px);
      grid-template-columns: 1fr;
      grid-template-rows: auto auto auto;
      gap: 0;
      height: auto;
    }

    .heritage__bottom-img:first-child {
      grid-column: 1;
      grid-row: 1;
      height: var(--heritage-bottom-mobile-image-height);
    }

    .heritage__bottom-img:last-child {
      grid-column: 1;
      grid-row: 3;
      height: var(--heritage-bottom-mobile-image-height);
    }

    .heritage__bottom-text {
      grid-column: 1;
      grid-row: 2;
      padding: var(--space-3);
    }
  }

  /* heritage__bottom レスポンシブ: 425px以下 → テキスト縮小 */
  @media (max-width: 425px) {
    .heritage__bottom-text {
      font-size: var(--text-base);
      padding: var(--space-4);
    }
  }

  @media (max-width: 425px) {
    .heritage__heading {
      font-size: clamp(2rem, 8.5vw, 2.8rem);
    }
  }

  @media (max-width: 320px) {
    .heritage__overlay-text {
      line-height: var(--leading-normal);
      word-break: normal;
      text-align: start;
      padding-inline: var(--space-3);
    }
  }

  /* ---------- 3.5 Services (仕事内容) ---------- */
  .services__inner {
    padding-block: var(--space-16);
    padding-inline: var(--space-6);
  }

  .services__heading {
    font-size: clamp(var(--text-xl), 2vw, 1.875rem);
    color: var(--color-white);
    text-align: right;
    overflow-wrap: break-word;
  }

  /* バナー + リード文の横並びコンテナ */
  .services__content {
    display: flex;
    gap: var(--space-10);
    align-items: flex-end;
    margin-bottom: var(--space-10);
  }

  /* リード文（縦書き・右側配置） */
  .services__lead {
    writing-mode: vertical-rl;
    flex-shrink: 0;
    overflow: visible;
    gap: 1rem;
    display: flex;
    flex-direction: column;
    margin-block: 0;
    mix-blend-mode: difference;
    color: var(--color-white);
  }

  .services__lead-title {
    font-size: var(--text-2xl);
    margin: 0 0 var(--space-4);
    margin-top: calc(var(--space-6) * -1);
  }

  .services__lead-text {
    font-size: var(--text-lg);
    line-height: var(--leading-normal);
    margin: 0;
    width: 5lh;
    height: 9lh;
  }

  /* 縦長バナー */
  .services__banners {
    --services-banner-fs: 2.5rem; /* 40px */
    --banner-w: clamp(120px, 15.8vw, 228px);
    --banner-h: clamp(400px, 43.2vw, 622px);
    --banner-gap: clamp(16px, 3.8vw, 55px);
    display: flex;
    flex-direction: row-reverse;
    gap: var(--banner-gap);
    flex: 1;
    isolation: isolate;
    translate: -5%;
  }

  .services__banner:nth-child(2) {
    margin-top: clamp(10px, 1.4vw, 20px);
  }
  .services__banner:nth-child(3) {
    margin-top: clamp(20px, 2.8vw, 40px);
  }
  .services__banner:nth-child(4) {
    margin-top: clamp(30px, 4.2vw, 60px);
  }

  .services__banner {
    --banner-label-size: clamp(1.5rem, 2.8vw, var(--services-banner-fs));
    --banner-inner-top: var(--space-8);
    --banner-inner-bottom: var(--space-4);
    --banner-inner-left: var(--space-3);
    --banner-inner-right: var(--space-6);
    position: relative;
    width: var(--banner-w);
    height: var(--banner-h);
    min-height: max(
      var(--banner-h),
      calc(
        var(--banner-inner-top) + (var(--banner-label-size) * 2.5) +
          var(--space-8)
      )
    );
    background-color: var(--color-dark);
    border-radius: 20px 0;
    box-shadow: 3px 5px 5px rgb(0 0 0 / 30%);
  }

  /* 本体画像 */
  .services__banner::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: var(--banner-bg);
    background-size: cover;
    background-position: center;
    border-radius: 20px 0;
    filter: grayscale(100%);
    z-index: 0;
  }

  /* 磨りガラス風シャドウ（右8px・上8px） */
  .services__banner::after {
    content: "";
    position: absolute;
    top: -8px;
    left: 8px;
    width: 100%;
    height: 100%;
    background-image: var(--banner-bg);
    background-size: cover;
    background-position: center;
    border-radius: 20px 0;
    filter: blur(1px);
    opacity: 0.45;
    z-index: 2;
  }

  .services__banner-inner {
    position: absolute;
    inset-block: var(--banner-inner-top) var(--banner-inner-bottom);
    inset-inline-end: var(--banner-inner-right);
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    max-inline-size: calc(
      100% - var(--banner-inner-left) - var(--banner-inner-right)
    );
    max-block-size: calc(
      100% - var(--banner-inner-top) - var(--banner-inner-bottom)
    );
  }

  .services__banner-label {
    writing-mode: vertical-rl;
    font-size: var(--banner-label-size);
    line-height: 1.1;
    color: var(--color-white);
    letter-spacing: 0.2em;
    white-space: normal;
    overflow-wrap: anywhere;
  }

  .services__banner-dash {
    display: block;
    width: 1px;
    flex: 1;
    min-height: 0;
    background-color: var(--color-white);
    transform: scaleY(0.9);
    transform-origin: top;
  }

  .services__more {
    width: fit-content;
    margin-left: auto;
  }

  /* Services レスポンシブ */
  @media (max-width: 1024px) {
    .services__content {
      flex-direction: column;
    }

    .services__banners {
      --banner-gap: 20px;
      width: 100%;
      translate: none;
    }

    .services__banner {
      width: 0;
      flex: 1;
      height: clamp(250px, 40vw, 400px);
      min-height: max(
        clamp(250px, 40vw, 400px),
        calc(
          var(--banner-inner-top) + (var(--banner-label-size) * 2.5) +
            var(--space-8)
        )
      );
    }

    .services__lead {
      align-self: flex-start;
      margin-block: var(--space-4);
    }

    .services__lead-text {
      max-height: clamp(200px, 30vw, 300px);
      font-size: var(--text-sm);
    }
    .services__heading {
      margin-bottom: var(--space-4);
    }
  }

  @media (max-width: 425px) {
    .services__banners {
      display: grid;
      grid-template-columns: 1fr;
      --banner-gap: 12px;
      --services-banner-fs: var(--text-xl);
    }

    .services__banner:nth-child(2),
    .services__banner:nth-child(3),
    .services__banner:nth-child(4) {
      margin-top: 0;
    }

    .services__banner {
      width: 100%;
      flex: none;
      height: 90px;
      min-height: max(
        90px,
        calc((var(--banner-label-size) * 1.4) + var(--space-10))
      );
      --banner-inner-top: var(--space-3);
      --banner-inner-bottom: var(--space-3);
      --banner-inner-left: var(--space-3);
      --banner-inner-right: var(--space-3);
    }

    .services__banner-inner {
      inset: var(--banner-inner-top) var(--banner-inner-right) auto
        var(--banner-inner-left);
      flex-direction: row;
      align-items: center;
      gap: var(--space-3);
      max-inline-size: calc(
        100% - var(--banner-inner-left) - var(--banner-inner-right)
      );
      max-block-size: none;
      direction: ltr;
    }

    .services__banner-label {
      writing-mode: horizontal-tb;
      line-height: 1.2;
      letter-spacing: 0.12em;
    }

    .services__banner-dash {
      width: auto;
      height: 1px;
      flex: 1;
      min-width: 0;
      min-height: auto;
      transform: scaleX(0.9);
      transform-origin: left;
    }

    .services__lead {
      writing-mode: horizontal-tb;
      text-align: center;
      gap: 0;
    }
    .services__lead-text {
      width: auto;
      max-height: none;
      line-height: var(--leading-normal);
      text-align: left;
    }
  }

  /* ---------- 共通セクション ---------- */
  .services,
  .works,
  .recruit,
  .contact {
    width: 100%;
  }

  .works {
    overflow: hidden;
  }

  .services {
    position: relative;
    background-image: url("/images/util/noise-tile-300.svg");
    background-repeat: repeat;
    background-size: 300px 300px;
    z-index: 0;
  }

  .services::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 50%;
    background-color: var(--color-dark);
    transform: skewY(-6deg);
    transform-origin: top left;
    z-index: -1;
  }

  .recruit__inner,
  .contact__inner {
    padding-block: var(--space-16);
    padding-inline: var(--space-6);
  }

  /* ---------- 4. Works ---------- */
  .works__header {
    width: 100%;
    min-height: clamp(3rem, 7vw, 6.25rem);
    display: flex;
    align-items: center;
    justify-content: center;
    border-top: 10px gray solid;
    border-bottom: 10px gray solid;
    background-color: #b9b9b9;
    background-image: url("/images/index/belt-pattern.svg");
    background-repeat: repeat-x;
    background-position: center;
    background-size: auto 100%;
  }

  .works__heading {
    margin: 0;
    font-size: clamp(var(--text-xl), 2.2vw, 2rem);
    font-weight: 700;
    line-height: 1;
    text-align: center;
    color: var(--color-black);
    overflow-wrap: break-word;
  }

  /* ---------- 5. Recruit ---------- */
  .recruit__inner {
    --recruit-cta-size: clamp(240px, 30vw, 348px);
  }

  .recruit__card {
    position: relative;
    max-width: 1307px;
    min-height: 823px;
    margin-inline: auto;
    border-radius: 50px;
    background-color: var(--color-gray-700);
    padding: var(--space-12) var(--space-10)
      calc(var(--recruit-cta-size) * 0.8 + var(--space-4));
    display: flex;
    flex-direction: column;
    isolation: isolate;
    filter: opacity(0.8);
  }

  /* 背景画像オーバーレイ: カード全面に敷くため absolute */
  .recruit__card::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: var(--recruit-bg);
    background-size: cover;
    background-position: center;
    border-radius: 50px;
    filter: grayscale(100%);
    z-index: 0;
  }

  /* 背景ぼかし影: カード外周にブラー効果を出すため absolute */
  .recruit__card::after {
    content: "";
    position: absolute;
    top: 0px;
    left: 0px;
    width: 100%;
    height: 100%;
    background-image: var(--recruit-bg);
    background-size: cover;
    background-position: center;
    border-radius: 50px;
    transform: scale(1.1, 1.13);
    filter: blur(5px);
    opacity: 0.45;
    z-index: -1;
  }

  .recruit__card > * {
    z-index: 2;
    min-width: 0;
  }

  .recruit__intro {
    max-width: 66%;
  }

  .recruit__heading {
    font-size: clamp(var(--text-xl), 4vw, var(--text-4xl));
    color: var(--color-white);
    margin: 0 0 var(--space-4);
    letter-spacing: 0.08em;
    overflow-wrap: break-word;
    padding-bottom: 0.3125rem;
    border-bottom: 1px solid #448aab;
    width: fit-content;
    max-width: 100%;
  }

  .recruit__desc {
    --recruit-desc-line-display: block;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    color: rgba(255, 255, 255, 0.85);
    font-size: var(--text-lg);
    font-weight: var(--weight-bold);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .recruit__desc-line {
    display: var(--recruit-desc-line-display);
    inline-size: max-content;
    max-inline-size: 100%;
    min-width: 0;
  }

  /* div-2: キャッチコピー — 右上縦書き。カード上に浮かせて配置するため absolute */
  .recruit__catchphrase {
    position: absolute;
    top: var(--space-10);
    right: var(--space-10);
    writing-mode: vertical-rl;
    color: var(--color-white);
    font-size: clamp(var(--text-xl), 2.8vw, var(--text-3xl));
    line-height: var(--leading-tight);
    margin: 0;
  }

  .recruit__catchphrase-break {
    display: block;
  }

  /* div-3: CTA 正円 — カード下辺から半分はみ出して浮かせるため absolute */
  .recruit__cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translate(-50%, 20%);
    width: var(--recruit-cta-size);
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 50%;
    outline: 2px solid var(--color-white);
    outline-offset: -2px;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: var(--color-white);
    font-size: clamp(var(--text-xl), 2.8vw, var(--text-2xl));
    letter-spacing: 0.04em;
    text-decoration: none;
    transition: background-color var(--duration-fast) var(--easing-default);
  }

  .recruit__cta:hover {
    background-color: rgba(92, 92, 92, 0.7);
  }

  .recruit__cta-text {
    text-align: center;
    font-size: clamp(1.5rem, 3vw, 3.125rem);
  }

  .recruit__cta :global(svg) {
    width: clamp(40px, 4vw, 72px);
    height: auto;
  }

  .recruit__cta :global(svg circle) {
    fill: var(--color-primary-light);
  }

  .recruit__cta :global(svg path) {
    stroke: var(--color-gray-500);
  }

  .recruit__cta-label {
    font-size: var(--text-sm);
    letter-spacing: 0.12em;
    line-height: var(--leading-tight);
    text-align: center;
  }

  .recruit__note {
    max-width: 682px;
    margin: calc(var(--recruit-cta-size) * 0.2 + var(--space-4)) auto 0;
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
    line-height: var(--leading-normal);
    text-align: center;
  }

  .recruit__note-line {
    display: block;
  }

  .recruit__note-break {
    display: block;
  }

  /* Recruit レスポンシブ: 1440px以下 */
  @media (max-width: 1440px) {
    .recruit__inner {
      max-width: 80%;
      --recruit-cta-size: clamp(200px, 20vw, 280px);
    }
    .recruit__card {
      min-height: calc((80vw - 3rem) * 823 / 1307);
    }
    .recruit__cta-text {
      font-size: 1.5rem;
      max-width: 8cap;
    }
  }

  /* Recruit レスポンシブ: 1024px以下 */
  @media (max-width: 1024px) {
    .recruit__inner {
    }

    .recruit__cta-text {
      font-size: 1.3rem;
      max-width: 8cap;
    }
  }

  /* Recruit レスポンシブ: 768px以下 */
  @media (max-width: 768px) {
    .recruit__inner {
      max-width: 90%;
    }

    .recruit__card {
      padding: var(--space-8) var(--space-6)
        calc(var(--recruit-cta-size) * 0.8 + var(--space-4));
      border-radius: 30px;
      min-height: 0;
    }

    .recruit__intro {
      max-width: 100%;
      margin-bottom: var(--space-4);
    }
    .recruit__desc {
      font-size: var(--text-base);
    }

    .recruit__catchphrase {
      position: relative;
      top: auto;
      right: auto;
      text-align: left;
      margin-bottom: var(--space-6);
    }

    .recruit__cta {
      margin-inline: auto;
      margin-bottom: var(--space-4);
    }

    .recruit__heading {
      font-size: clamp(var(--text-lg), 4vw, var(--text-3xl));
    }
    .recruit__note {
      margin: calc(var(--recruit-cta-size) * 0.2 + var(--space-4)) auto 0;
    }
  }

  /* Recruit レスポンシブ: 425px以下 */
  @media (max-width: 425px) {
    .recruit__inner {
      max-width: 100%;
    }

    .recruit__card {
      border-radius: 24px;
      padding: var(--space-6) var(--space-4)
        calc(var(--recruit-cta-size) * 0.8 + var(--space-4));
    }

    .recruit__desc {
      font-size: var(--text-base);
    }

    .recruit__note {
      font-size: var(--text-xs);
    }
  }

  /* Recruit レスポンシブ: 320px以下 */
  @media (max-width: 320px) {
    .recruit__card {
      padding-bottom: var(--space-6);
    }

    .recruit__note {
      margin-top: var(--space-4);
    }

    .recruit__cta {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      grid-template-rows: auto auto;
      gap: var(--space-4);
      width: 100%;
      aspect-ratio: auto;
      overflow: visible;
      border-radius: 20px;
      transform: unset;
      left: 0;
      position: relative;
      padding: var(--space-4);
      overflow-wrap: break-word;
    }

    .recruit__cta-text {
      grid-column: 1 / -1;
      grid-row: 1;
      max-width: none;
      font-size: 1.1rem;
    }

    .recruit__cta-label {
      grid-column: 1;
      grid-row: 2;
    }

    .recruit__cta :global(svg) {
      grid-column: 2;
      grid-row: 2;
      display: grid;
      align-content: center;
    }
  }
  .recruit__card::after {
    transform: scale(1.15, 1.05);
  }
  /* ---------- 6. Contact ---------- */
  .contact__inner {
    display: block;
    color: inherit;
    text-decoration: none;
    background-image: url("/images/util/contact-bg.svg");
    background-repeat: no-repeat;
    background-size: cover;
    background-position: center;
    --icon-arrow-circle-fill: var(--color-primary-light);
    --icon-arrow-path-stroke: var(--color-gray-500);
    --icon-arrow-border: rgba(0, 0, 0, 0.5);
    --icon-arrow-circle-fill-hover: var(--color-gray-500);
    --icon-arrow-path-stroke-hover: var(--color-primary-light);
    --icon-arrow-border-hover: rgba(255, 255, 255, 0.5);
  }

  .contact__card {
    max-width: min(1320px, 100%);
    min-height: clamp(200px, 28vw, 400px);
    margin-inline: auto;
    border-radius: var(--contact-card-radius);
    background-color: color-mix(
      in srgb,
      var(--color-gray-500) 90%,
      transparent
    );
    display: grid;
    grid-template-columns: 1fr auto;
    grid-template-rows: auto 1fr;
    grid-template-areas:
      "title title"
      "desc  more";
    align-items: end;
    padding: var(--contact-card-radius);
    gap: var(--space-6);
  }

  .contact__title-group {
    grid-area: title;
  }

  .contact__heading {
    font-size: clamp(2rem, 8vw, 3.75rem);
    white-space: normal;
    overflow-wrap: anywhere;
    color: var(--color-white);
    margin: 0;
    letter-spacing: 0.08em;
    text-decoration: underline;
    text-decoration-color: var(--color-primary);
    text-underline-offset: 0.25em;
    text-decoration-thickness: 0.1875em;
  }

  .contact__heading-sub {
    display: block;
    margin-top: 0.5em;
    font-size: var(--text-lg);
    color: var(--color-white);
  }

  .contact__desc {
    grid-area: desc;
    color: rgba(255, 255, 255, 0.85);
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .contact__desc-break {
    display: block;
  }

  .contact__more {
    grid-area: more;
    width: fit-content;
  }

  @media (max-width: 1024px) {
    .contact__card {
      grid-template-columns: 1fr;
      grid-template-rows: auto auto auto;
      grid-template-areas:
        "title"
        "desc"
        "more";
      height: auto;
      align-items: start;
    }
  }
  @media (max-width: 768px) {
    .contact__more {
      justify-self: end;
    }
  }
</style>
