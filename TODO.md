# ToDoリスト (Task List)

プロジェクトの現状に基づいた残タスクと改善案のリストです。

## 優先度: 高 (High Priority)

- [ ] **ロゴ画像の配置**
    - 現状: `/img/logo.gif` が404エラー（プレースホルダーで代用中）。
    - 対応: 正しいロゴ画像を `public/img/` に配置するか、パスを修正する。

## 優先度: 中 (Medium Priority)

- [ ] **レスポンシブ対応の確認と調整**
    - 現状: デスクトップ向けの固定ヘッダー実装のみ。
    - 対応: スマホ画面でのハンバーガーメニュー実装や、表示崩れの確認。
- [ ] **SEO / Metaタグの実装**
    - 現状: `Layout.astro` の `<title>` や `<meta>` が固定値。
    - 対応: 各ページから props でタイトルや説明文を受け取れるように改修する。

## 優先度: 低 (Low Priority)

- [ ] **画像の最適化**
    - 現状: `<img>` タグを使用。
    - 対応: Astroの `<Image />` コンポーネント (`astro:assets`) を導入し、自動最適化（WebP変換、サイズ調整）を行う。
- [ ] **パンくずリスト (Breadcrumbs) の実装**
    - 対応: ユーザーが現在位置を把握しやすくするため、各ページ下層にパンくずリストを追加する。
- [ ] **404ページの作成**
    - 対応: `src/pages/404.astro` を作成し、存在しないURLにアクセスした際のカスタムページを用意する。

## 完了済み (Completed)

- [x] **共通ヘッダーの作成**: `header.astro` を作成し、全ページに適用。
- [x] **ヘッダーの固定表示**: `position: fixed` と `Layout` への余白追加。
- [x] **ドロップダウンメニューのアクセシビリティ**: フォーカス外れ時に自動で閉じるJS (`details-utils.ts`) を実装。
- [x] **サイト構造データの定義**: `src/lib/site-data.ts` 作成。
- [x] **サイトマップページの作成**: `src/pages/sitemap.astro` 作成。
